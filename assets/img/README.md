Put a square portrait here named `avatar.jpg` (600×600 or larger).

It replaces the "GW" monogram on the homepage automatically — no code change
needed. If this file is absent the monogram simply stays, so the page never
shows a broken image.
